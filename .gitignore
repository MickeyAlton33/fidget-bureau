# macOS
.DS_Store

# editor / scratch
*.swp
*~
scratchpad/

# node (there are no deps, but just in case)
node_modules/
npm-debug.log*

# build output — dist/ is regenerated by `node build.js`.
# docs/ is intentionally committed: GitHub Pages serves docs/index.html.
dist/
